/**
 * The sub-navigation is not admin-specific — the account area uses it too — so it lives in
 * `elements` now. Re-exported here so the existing admin imports keep working.
 */
export { SubNavigation, SubNavigationLink } from '@/elements/SubNavigation';
